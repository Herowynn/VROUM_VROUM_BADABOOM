/// <summary>
/// PlayerState is an enum that is used in every player scripts.
/// For example it will prevent the player from moving when he is dead.
/// </summary>

public enum PlayerState
{
    ALIVE,
    DEAD
}